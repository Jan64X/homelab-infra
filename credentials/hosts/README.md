# Host-Specific Credentials Directory

This directory contains credentials generated for each managed host.

## Structure

Each host will have its own subdirectory named after the inventory hostname:

```
hosts/
  └── hostname/
      ├── root_pass.txt
      ├── ansible_pass.txt
      ├── sysadmin_pass.txt
      ├── salts/
      │   ├── root_salt.txt
      │   ├── ansible_salt.txt
      │   └── sysadmin_salt.txt
      └── [service-specific credentials]
```

## Service-Specific Credentials

Some hosts may have additional credential files depending on the services they run:

### Authentication & Access
- `grafana_admin_password.txt` - Grafana admin password
- `initial_password.txt` - Initial password for services like qBittorrent
- `mongo_root_password.txt` - MongoDB root password
- `mongo_unifi_password.txt` - MongoDB UniFi user password

### Backup Encryption (Restic)
- `restic_unifi_password.txt` - UniFi Controller backup encryption key
- `restic_navidrome_password.txt` - Navidrome backup encryption key
- `restic_observability_password.txt` - Observability stack backup encryption key

**Important**: Restic passwords encrypt your backups. Keep them safe! Without them, backups cannot be restored.

These are automatically generated by Ansible playbooks and stored securely. Do not delete unless performing a fresh installation.
